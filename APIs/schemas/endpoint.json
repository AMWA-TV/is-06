{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "version": {
      "description": "String formatted TAI timestamp (<seconds>:<nanoseconds>) indicating precisely when an attribute of the endpoint last changed",
      "type": "string",
      "pattern": "^[0-9]+:[0-9]+$"
    },
    "label": {
      "description": "Freeform string label for the endpoint",
      "type": "string"
    },
    "description": {
      "description": "Detailed description of the endpoint",
      "type": "string"
    },
    "tags": {
      "description": "Key value set of freeform string tags to aid in filtering endpoints, if necessary. Values should be represented as an array of strings. Can be empty.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "chassis_id": {
      "description": "Chasis id of the endpoint. LLDP is one way the chassid id should be passed to the network. For virtual system it can be null.",
      "type": "string"
    },
    "port_id": {
      "description": "Port ID of the endpoint. Similar to chassis-id, it can be advertised in LLDP from the endpoint.",
      "type": "string",
      "pattern": "^([0-9A-Fa-f]{2}[-]){5}([0-9A-Fa-f]{2})$"
    },
    "network-device-id": {
      "type": "string",
      "description": "Network device Id or switch id. If not provided, it's assumed to be discovered using other protocols or out of band.",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "network-interface": {
      "type": "string",
      "description": "Network device interface or switch interface. If not provided, it's assumed to be discovered using other protocols or out of band."
    }
  },
  "required": [
    "id",
    "version",
    "port_id"
  ]
}
